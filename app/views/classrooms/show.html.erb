Classroom Info
<p>
<%= @classroom.course_name %>
<%= @classroom.semester %>
</p>

We now want to call out the oldest student in the class:
<%= render 'students/student' %>
<%= render partial: "students/student", locals: {classroom_student: @classroom.students.each do |student| {student.name} } %>
